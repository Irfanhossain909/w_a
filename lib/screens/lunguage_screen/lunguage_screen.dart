import 'package:flutter/material.dart';
import 'package:w_a/widgets/appbar/custom_appbar.dart';

class LunguageScreen extends StatelessWidget {
  const LunguageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: CustomAppBar(title: "Lunguages"),
      body: Column(
        children: [
          Container(
            
          )
        ],
      ),
    );
  }
}